export function moveTriangle(from: string, to: string, boardState: any): boolean {
    // Tarkista onko siirto sallittu triangle-nappulalle ja palauta true tai false
    return true;
  }
  
  export function moveSquare(from: string, to: string, boardState: any): boolean {
    // Tarkista onko siirto sallittu square-nappulalle ja palauta true tai false
    return true;
  }
  
  export function moveCircle(from: string, to: string, boardState: any): boolean {
    // Tarkista onko siirto sallittu circle-nappulalle ja palauta true tai false
    return true;
  }
  
  export function moveDiamond(from: string, to: string, boardState: any): boolean {
    // Tarkista onko siirto sallittu diamond-nappulalle ja palauta true tai false
    return true;
  }
  